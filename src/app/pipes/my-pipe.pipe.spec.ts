/* tslint:disable:no-unused-variable */

import { MyPipePipe } from './thePipe.pipe';

describe('Pipe: MyPipee', () => {
  it('create an instance', () => {
    let pipe = new MyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
